# Vagrant-to-10xLab compiler

Requirements:

* functional vagrant setup on local machine
* installed/configured GIT (user.name/user.email)

Example usage

	./vaglab.rb ~/Path/to/directory/with/Vagrant_file/

